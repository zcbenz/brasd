这是一个BRAS客户端，分为底层服务端brasd和上层图形端bras-client。

brasd是对xl2tpd进行了包装，通过分析xl2tpd的输出来获知网络状态，然后在其上
提供针对BRAS的接口。bras-client则使用其接口提供了一个简单的连接界面。

如果使用过程中遇到了Bug或者有新的建议，请到虎踞龙蟠BBS <bbs.seu.edu.cn>
的Linux版块留言。

你可以使用下面的命令进行编译、安装
./configure --prefix=/usr
make
sudo make install
sudo make install-service

