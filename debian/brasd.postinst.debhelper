# Automatically added by dh_installinit
if [ -x "/etc/init.d/brasd" ]; then
	update-rc.d brasd defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d brasd start || exit $?
	else
		/etc/init.d/brasd start || exit $?
	fi
fi
# End automatically added section
